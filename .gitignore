node_modules/
dist/
build/
.next/
coverage/
*.log
npm-debug.log*

# env
.env
.env.local
.env.*.local

# Sample DICOM. The committed set is public, de-identified test data
# (datalad example T1 MRI + rubomedical XA/US/RF demos) so the hosted
# deployment has something to show. NEVER add real patient data here.
sample-data/*.tmp

# orthanc / postgres volumes if bind-mounted
.data/
orthanc-db/
pgdata/

# editor
.vscode/
.idea/
.DS_Store
Thumbs.db

# JSON store (dev database)
/data/
apps/api/data/
